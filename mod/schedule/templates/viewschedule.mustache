<button type="button" class="btn btn-primary" onclick="location.href='{{backUrl}}'">Back To Course</button>
<br><br>
<div class="content-container">
    <b>{{scheduleName}}</b>
    <br><br>
    {{^checklocalschedule}}
    Your team has not chosen the schedule yet!<br>
    <button class="btn btn-primary" type="button" id="call_modal" value="{{eventid}}">Choose Your Team Schedule</button>
    {{/checklocalschedule}}
    {{#checklocalschedule}}
    <table class="generaltable">
        <tr>
            <td>Date</td>
            <td>{{#userdate}} {{selecteddate}}, {{#str}} strftimedate, core_langconfig {{/str}} {{/userdate}}</td>
        </tr>
        <tr>
            <td>Time</td>
            <td>{{selectedtime}}</td>
        </tr>
    </table>
    {{#allowedtocheckin}}
    
        {{^checkintime}}
        <button type="button" class="btn btn-secondary" id="check_in_button" value="{{id}}">check-in now</button>
        {{/checkintime}}

        {{#checkintime}}
        <i>you've checked in on {{checkintime}}</i> 
        {{/checkintime}}

    {{/allowedtocheckin}}
    {{/checklocalschedule}}
</div>
